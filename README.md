# Resume Vault

This repository is managed by Resume Vault. Each `categories/<slug>/` folder is a job category; each `.tex` or `.pdf` file is a resume version, with a sibling `.json` for metadata. Edits to checkpoint history are tracked as git commits.
