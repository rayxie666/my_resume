// React-PDF resume template. Edit freely, then save.
// See https://react-pdf.org for components & styling.
import { Document, Page, Text, View, StyleSheet } from "@react-pdf/renderer";

const styles = StyleSheet.create({
  page: { padding: 40, fontSize: 11, fontFamily: "Helvetica" },
  h1: { fontSize: 20, marginBottom: 4 },
  h2: { fontSize: 13, marginTop: 12, marginBottom: 4, borderBottom: 1 },
  row: { flexDirection: "row", justifyContent: "space-between" },
});

export default function Resume() {
  return (
    <Document>
      <Page size="LETTER" style={styles.page}>
        <Text style={styles.h1}>Your Name</Text>
        <Text>email · phone · city</Text>
        <Text style={styles.h2}>Experience</Text>
        <View style={styles.row}>
          <Text>Company — Role</Text>
          <Text>2024 – Present</Text>
        </View>
        <Text>• Bullet describing impact.</Text>
      </Page>
    </Document>
  );
}
